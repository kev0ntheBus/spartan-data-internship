# Data

This folder holds small sample data and tracked placeholders.

## Rules

- Do not commit large raw datasets.
- Do not commit private data.
- Keep `data/raw/` and `data/processed/` tracked with `.gitkeep`.
- Use `data/sample/` for tiny learning datasets.
